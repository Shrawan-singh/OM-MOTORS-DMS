'use client';
import Link from 'next/link';
import { useDealerStore } from '@/lib/store/dealer-store';
import { useAuth } from '@/lib/auth/provider';
import { money } from '@/lib/billing/types';
import { Users,Package,FileText,Receipt,BookOpen,Wrench } from 'lucide-react';
export default function Dashboard(){const {customers,documents,products,inventory,serviceJobs}=useDealerStore();const {can}=useAuth();const invoices=documents.filter(d=>d.kind==='invoice');const links=[['catalogue','Product catalogue',BookOpen],['customers','Customers',Users],['inventory','Inventory',Package],['quotations','Quotations',FileText],['invoices','Invoices & payments',Receipt],['service','Service & PDI',Wrench]] as const;return <div><div className="page-heading"><div><p className="eyebrow">OM MOTORS WORKSPACE</p><h1>Dealership overview</h1><p>Your saved dealership records, connected through Supabase.</p></div>{can('quotations.write')&&<Link className="primary-button" href="/quotations/new">New quotation</Link>}</div><div className="admin-stats"><div><Receipt/><strong>{can('invoices.read')?money(invoices.reduce((s,i)=>s+i.amount_paid,0)):'—'}</strong><span>Payments received</span></div><div><FileText/><strong>{can('invoices.read')?money(invoices.reduce((s,i)=>s+i.total-i.amount_paid,0)):'—'}</strong><span>Outstanding invoices</span></div><div><Users/><strong>{can('customers.read')?customers.length:'—'}</strong><span>Customers</span></div></div><div className="products-grid">{links.filter(([key])=>can(`${key}.read`)).map(([key,label,Icon])=><Link href={`/${key}`} key={key} className="admin-panel dashboard-link"><Icon size={24}/><h2>{label}</h2><p>{key==='catalogue'?`${products.length} catalogue variants`:key==='inventory'?`${inventory.length} stock records`:key==='service'?`${serviceJobs.length} service jobs`:'Open workspace'} →</p></Link>)}</div><section className="admin-panel mt-6"><h2 className="font-semibold mb-5">Recent documents</h2>{documents.slice(0,6).map(d=><Link className="recent-document" key={d.id} href={`/${d.kind==='invoice'?'invoices':'quotations'}/${d.id}/print`}><span>{d.number}<small>{d.customer_snapshot.name}</small></span><strong>{money(d.total)}</strong></Link>)}{!documents.length&&<p className="text-sm text-slate-500">No saved quotations or invoices yet.</p>}</section></div>}
